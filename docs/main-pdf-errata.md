# 主檔《機器學習模型圖卡(全).pdf》排版錯誤記錄

> 來源檔:`C:\Users\marco\Desktop\機器學習模型圖卡(全)20260516.pdf`(56 頁)
> 確認方法:orchestrator 透過 PyMuPDF render PNG + 直接 Read 視覺對比
> 日期:2026-05-18

## 為何記錄

主檔由作者「戴志宇」製作,共 56 頁(1 封面 + 1 心智圖 + 54 模型卡)。
經視覺核對發現 **2 處標題與內容不符的排版錯誤**;若未來重派 worker 看主檔生題,
worker 可能被誤導(寫錯內容)。本檔保存對照,**KB 已自行寫對,不抄主檔錯誤**。

---

## 錯誤 #1:Page 17(決策樹基礎型)— 內容完全 = Page 16 NB

### 應有內容
標題卡:**決策樹(基礎型)** / Decision Tree
應出現:if-else 規則樹、資訊增益 / Gini、二元分割

### 主檔實際內容(視覺檢查)
- ✅ 標題正確改為「決策樹(基礎型)」
- ❌ 核心觀念圖解:**完全 = page 16 NB**(顯示 P(類別|特徵) ∝ P(特徵|類別) × P(類別)、樸素獨立假設)
- ❌ 觀念速讀卡:仍寫「基於貝氏定理 + 屬性條件獨立假設」
- ❌ 易混淆對照:仍寫「樸素貝氏 vs 決策樹」
- ❌ 一句話總結:仍寫「獨立假設下的機率分類模型」(這是 NB 描述,不是決策樹!)

### 推測原因
作者複製 page 16 NB 範本,只改了標題卡,沒改內容。

### 影響評估
- 對使用者(看 PDF 自學)→ 嚴重誤導:會以為決策樹 = NB
- 對我們 KB → 無影響(`n_L23202_005` 決策樹節點自寫,引用其他課程資料)

---

## 錯誤 #2:Page 42(多層感知機 MLP)— 內容是 DQN

### 應有內容
標題卡:**多層感知機(MLP)** / Multi-Layer Perceptron
應出現:多層神經元、非線性激活、ReLU/Sigmoid、解決感知機無法分非線性問題

### 主檔實際內容(視覺檢查)
- ✅ 標題正確改為「多層感知機 (MLP)」「MLP 就是非線性分類與數據預測的基礎網路架構」
- ❌ 觀念速讀卡關鍵詞:寫「神經網路、狀態空間、Q Table 更新、最佳動作、自動化學習」← **全是 RL!**
- ❌ 核心圖解:Q-Learning Agent + Atari 遊戲 + 機械臂 + 自動駕駛 + 訓練階段 + 網路更新 + Reward + 折扣因子 × max Q(s', a')
- ❌ 使用情境:寫「看到 Q-Learning 的替代品」「Atari 遊戲」「機械臂控制」「自動駕駛」← **DQN 情境**
- ✅ 易混淆對照:「感知機(線性) vs 多層感知機(非線性)」(此項對應 MLP 主題正確)
- ❌ 一句話總結:「阿塔里遊戲,優先想到多層感知機」← **錯!Atari 是 DQN 經典**

### 推測原因
作者複製 page 40 DQN 範本,只改了標題卡 + 易混淆對照,沒改主視覺與一句話總結。

### 影響評估
- 對使用者(看 PDF 自學)→ 嚴重誤導:會以為 MLP 在 Atari 上跑
- 對我們 KB → 無影響:
  - `n_L23201_006` DQN 節點已正確寫(W8 / orchestrator 補)
  - MLP 節點(Tier B 未加)未受影響

---

## 主檔可信度與使用建議

- **可信度**:Page 1-16, 18-41, 43-56 的標題與內容對應正確(視覺抽樣確認 12 頁)
- **使用原則**:
  - **不可直接抄主檔頁面內容生題**(已知 2 處錯誤,未抽到的頁面也可能有類似問題)
  - **要用主檔內容,先視覺對照「標題 vs 核心圖解 vs 易混淆 vs 一句話」是否一致**
  - 一致才用;不一致以**標題類別**為主、**核心圖解**為輔重新編寫
- **如何驗證主檔某頁是否可信**:
  ```python
  # PyMuPDF render
  import fitz
  doc = fitz.open(PDF_PATH)
  doc[N-1].get_pixmap(dpi=120).save('check.png')
  # 然後人工或 visual agent 對照標題卡與核心圖解
  ```

---

## KB 對應節點(已正確,不抄主檔錯誤)

| 主檔頁 | 主檔內容 | KB 節點(內容正確) |
|---|---|---|
| 16 | Naive Bayes(正確) | `n_L23202_014` ✓ |
| 17 | **(標題決策樹但內容 NB)** | `n_L23202_005` 決策樹(內容自寫不抄主檔)✓ |
| 40 | DQN(正確) | `n_L23201_006` ✓ |
| 42 | **(標題 MLP 但內容 DQN)** | MLP 尚未加(Tier B) / DQN 用 page 40 為來源 ✓ |

## 後續行動

- 此檔長期保留作為「主檔審查紀錄」
- 若日後加 MLP 節點(Tier B),**不可從 page 42 抓素材**,改用 ML 經典教科書或其他 PDF
- 若日後加決策樹進階節點,**不可從 page 17 抓素材**,用 page 10 決策樹迴歸(內容正確)+ 既有 KB
